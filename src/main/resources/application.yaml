spring:
  application:
    name: docker
  profiles:
    active:
      - dev
  output:
    ansi:
      enabled: ALWAYS
  threads:
    virtual:
      enabled: true
  task:
    execution:
      mode: FORCE

